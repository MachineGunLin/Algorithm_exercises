#include<bits/stdc++.h>
using namespace std;

int main() {
    int X;
    while(cin >> X && X) {
        for(int i = 1; i < X; ++i) {
            cout << i << " ";
        }
        cout << X << endl;
    }
}
